'use strict';

// function labelsCheck(nodeList, count) {
//   let counter = count;
//   nodeList.forEach(label => {
//     label.id = `label-${counter}`;
//     counter++;

//     label.onkeydown = event => {
//       if (event.key == ' ') {
//         document.getElementById(label.id).click();
//       }
//     };
//   });
// }
// export default labelsCheck;
